//Escreva uma função soma que recebe dois parâmetros e devolve a soma dos dois parâmetros.

const { number1, number2 } = { number1: 40, number2: 2 };

const soma2 = (a, b) => a + b;

console.log(soma2(number1, number2));
